[package]
name = "project-canon-core"
description = "Two-layer conformance model (base canon + archetype profiles) for project-canon."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# Zero external dependencies at v0. serde (JSON serialization of a Resolution, for the §10
# doctor/review payloads) is a deferred seam — added when a verb consumes it, not speculatively.
[dependencies]

[lints]
workspace = true
