### Added

- Added canon §23 and a `doctor` gate that detects configured private-environment markers in publicly distributed artifacts while allowing the project's own public coordinates. The deny-list is supplied through user configuration, and `review` retains the hostname, internal-URL, and borderline-naming judgment pass.
