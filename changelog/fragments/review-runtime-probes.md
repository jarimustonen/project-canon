### Added

- Add opt-in `review --run <binary>` checks for the runtime-observable CLI canon surfaces, with direct non-shell execution, bounded output, per-call timeouts, read-only argument vectors, and explicit `could-not-probe` outcomes.
